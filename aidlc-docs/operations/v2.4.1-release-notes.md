# SpoolPainter v2.4.1 — release notes

**versionCode 116 · versionName 2.4.1 · updated 2026-08-28**
Two bugfixes plus one UI change on top of 2.4.0. Carries **U26 (UI-63)**,
**UI-65** and **U27 (UI-67)**.

> **Why no version bump for U27.** 116 / 2.4.1 was bumped for U26 + UI-65 but
> **never uploaded**, so it is still an unshipped code. U27 folds into it rather
> than consuming 117. Nothing is gained by burning a code on an unshipped build.

> **versionCode note.** 115 / 2.4.0 is tagged and GitHub-released, so it is treated
> as consumed whether or not it reached Play. 116 is the next free code. Confirm in
> Play Console before upload — git tags are not a record of Play uploads, and
> reusing a consumed code is what got 113 rejected.

---

## Play Console release notes (paste as-is)

**Play caps this field at 500 characters per language**, and the two tracks are on
**different versions**, so they need **different notes**. Do not concatenate them,
and do not reuse the per-version note below.

### Why a per-version note is wrong here

Play release notes attach to a **release**, per track, per language. Play **never
accumulates** the notes of versions a user skipped: a user sees only the note on the
latest release for their own track. So anything shipped in a version that never
reached that track is invisible unless this note carries it.

**CORRECTED 2026-08-28 by the maintainer: production is on 2.3.1, not 2.2.1.** The
2.4.0 notes doc says 2.2.1 and that is now stale — 2.3.0 and 2.3.1 did reach
production. Anything derived from the 2.2.1 baseline is wrong: an earlier draft of
this block advertised type-to-search, read-time suggestions and the 16 KB page-size
fix as new, and stable users have had all three since 2.3.1.

| Track | Users are on | This note must cover |
|---|---|---|
| Open testing | **2.3.2** | 2.4.0 + 2.4.1 |
| Production | **2.3.1** | 2.3.2, 2.4.0, 2.4.1 |

Neither list fits in 500 characters as an enumeration, so both are written by
**user-visible theme**, not per unit. Items a track already has are deliberately
absent.

### Open testing — testers are on 2.3.2 (114). 450 characters.

```
Reuse a filament as a template
• Clearing the filament with the X only unlinks it. Every value stays, so pick a similar one, change the color and save it as new.
• A Clear button in the top left resets everything.

Smarter pickers
• Pick a brand and matching filaments float to the top.
• Search matches brand and material together, so "3dhojor petg" finds it.

Brands
• The brand you type is written exactly as you typed it, with no re-capitalising.
```

### Production — stable users are on 2.3.1. 486 characters.

Covers 2.3.2 + 2.4.0 + 2.4.1. Type-to-search, read-time suggestions, the longer
variant field and the 16 KB fix are **deliberately absent — production already has
them.**

```
• The filament X only unlinks it, so a similar filament is a template. A new Clear button resets everything.
• Search matches brand and material together, so "3dhojor petg" finds it.
• Matching filaments float to the top from the brand you pick, not just after a tag read.
• Brands are written exactly as you type them.
• A selected spool refreshes from Spoolman without losing your edits.

Fixes: dots in Variant, duplicate brands merged, Settings in one tap, the missing write prompt.
```

**Three items did not fit**, in the order I would trade them back in:
1. **Shop links in Settings** (the referral card). A variant carrying it lands at
   exactly 500 characters, i.e. zero headroom, so it is not offered as the default;
   trading out the `Fixes:` line makes room safely. This is a revenue item, so it is
   the maintainer's call, not mine.
2. **The Brand list uses your own Spoolman's spelling** — the second half of the
   brand fix. In the README.
3. **The What's-new sheet no longer repeats** — invisible by nature.

### Deliberately cut, and why

- **The ⋮ opening Settings directly** is in neither. It is a one-tap saving on a
  screen nobody visits twice, and it costs characters that the template-reuse and
  search items use better. It is in the README for anyone who looks.
- **The no-Spoolman write prompt fix** is in neither: users who hit it thought the
  button was broken, and describing it re-raises a doubt most people never had.
- **"Existing filaments keep their names"** survives only in the README. It matters
  to the #8 reporter and to anyone with sloppy vendor casing, but it is a caveat, and
  480 characters of a 500 budget is not where a caveat wins over a feature.
- **The 16 KB page-size fix** (2.3.1) is dropped from the production list this time.
  It was in 2.4.0's draft; it is invisible to users and the space is better spent.

### Per-version note (for reference only, do NOT paste into Play)

Scoped to 2.4.1's own changes. Useful for the GitHub Release body, where there is no
character limit and the audience is reading a changelog rather than a store page.
494 characters.

```
Brand names are written exactly as you type them
• Type a brand under Other and that spelling goes on the tag and into Spoolman.
• The Brand list uses your own Spoolman's spelling.
• Filaments you already saved keep their names. Only new ones change.

Quicker to clear
• A Clear button in the top left empties the form in one tap. It greys out when nothing needs clearing.
• The top right icon opens Settings directly.

Fixes
• Writing a tag with no Spoolman shows the "Tap a tag" prompt again.
```

---

## What is in it

### U26 — the brand you choose is the brand that gets written (UI-63)

Reported on GitHub #8: "Brand names (mostly) get all capital when written to the NFC
tag. Jayo becomes JAYO. NextShapes stays NextShapes."

Two changes, which had to land together or the app contradicts itself:

1. **Write path.** `resolveBrandName` rewrote the brand to whatever built-in entry
   matched case-insensitively, so a typed "Tecbears" reached the tag as "TECBEARS"
   while the form still displayed "Tecbears". Now a pass-through; whitespace is the
   only thing normalised.
2. **Brand dropdown.** `mergeBrands` listed presets first and deduped on
   `lowercase()`, so a vendor the user created as "Tecbears" was not merely
   deprioritised, it was **unpickable** — the preset "TECBEARS" shadowed it. Server
   vendors now come first verbatim; a preset is offered only when no vendor matches
   it case-insensitively.

**The justification for the old behaviour was false.** Its comment claimed Spoolman
dedupes vendor rows case-insensitively. Checked against Spoolman's schema:
`vendor.name` is `mapped_column(String(64))` with no unique constraint, index or
collation. It dedupes nothing.

Vendor *records* are still never renamed — `resolveOrCreateVendor` keeps its
case-insensitive match-and-reuse, because renaming rewrites a row every other
filament of that brand points at.

### U27 — dedicated "Clear" button, and ⋮ opens Settings directly (UI-67)

Maintainer request. `onClearAll()` already existed but was reachable only from the
`⋮` overflow menu, so this promotes an existing action to its own control.

- **Filled tonal "Clear" pill at the header's top left.** This reverses UI-57,
  which had put clear-all *into* the menu after three in-header variants crowded
  the logo on device. That rejection was location-specific: all three sat at
  `TopEnd`, and `TopStart` was never tried. Measured on device, the left gutter is
  ~182 px clear at default font and still 45 px at `font_scale` 2.0, single-line
  throughout.
- **Label is "Clear", not "Clear all"**, and padding is tightened from M3's 24 dp
  default. Both are width decisions forced by that 45 px measurement.
  `contentDescription` keeps "Clear all fields" for screen readers.
- **The `⋮` icon stays put but now opens Settings in one tap.** Once clear-all
  moved out, the menu held a single row, so the popup was a tap in the way of the
  only thing behind it. The glyph is deliberately unchanged — swapping it for a
  gear was tried and reverted.
- **The button greys out when clearing would change nothing.** The predicate is
  derived, not guessed: pure `MainUiState.cleared()` is the single definition of a
  clear, `onClearAll` applies it and `canClear` asks whether applying it would
  alter state, so the button and the action cannot drift.

### UI-65 — no status overlay when writing without Spoolman

`computeStatusLabel` had no branch for `ActiveFlow.WritingRaw`, the flow reached only
when no Spoolman URL is set, so `NfcStatusOverlay` never appeared: no "Tap a tag to
write" caption and no animation, while the button *did* flip to Cancel. Found by the
maintainer on device during the U26 install gate.

## Verification

- **Tests 615 → 643** (U26 +9, UI-65 +7, U27 +12). 0 failures. Baseline at 2.4.0
  was 615.
- Both fixes validated by reverting only the production files and re-running:
  8 of 10 fail for U26, 4 of 7 for UI-65. Neither test set passes vacuously.
- **U27's tests validated by breaking the production code**, not by re-reading
  assertions: forcing `canClear` to `true` fails 2 tests (the two pinning
  greying-out), and removing the custom-name-buffer fold fails **exactly** the 2
  tests written for that seam.
- **Install gate PASSED** on moto g stylus 2025 / Android 16 against a live Spoolman
  (22 vendors, 113 spools). Full evidence in the U26 plan §8, U27's in its own
  plan §8 and §13.
- No Spoolman records created, modified or deleted during testing. The URL was
  cleared and byte-exact restored twice, verified each time.

## Say this in the release notes, not in a support thread

**Old filament records keep their old names.** A user whose Spoolman already holds
`TECBEARS PLA Grau` will not see it renamed — the app deliberately does not
bulk-rename. Only newly created records pick up the corrected spelling. The Play
copy above carries this line for exactly that reason.

**Your dropdown may change spelling.** Where a user's own Spoolman vendor differs in
case from a built-in name, the dropdown now shows theirs. On the maintainer's own
server that changed five rows (`3DHoJor`→`3DHojor`, `Elegoo`→`ELEGOO`,
`eSUN`→`eSun`, `JAYO`→`Jayo`, `SUNLU`→`Sunlu`). Three of those are arguably worse
than the built-in styling — that is server-wins working as designed, and the remedy
is renaming the vendor row in Spoolman once, not softening the rule.

## Known / deliberately unchanged

- **UI-64 (NEW, not fixed)** — blank NTAGs that have never been NDEF-formatted are
  classified as vendor tags and refused, with the write never attempted. Root cause
  **proven in code**; regression pinned to v2.1.1 (U14b vendor expansion). This is
  the real cause of the recurring "NFC is hit and miss" reports. **Strongest
  candidate for the next unit.** Note the reporter currently says it works, which is
  expected — tags rescued with NFC Tools stay formatted.
- **UI-53** — still open and distinct from UI-64. A new mechanism was observed on
  device this session (`BAL_BLOCK` on the foreground-dispatch PendingIntent, which
  got through only because the activity had a visible window). Not confirmed, but it
  fits the symptom better than the parked theory and comes with a candidate fix.
- **`resolveMaterialName` still canonicalises** against the material presets
  (U26 §7 R2). Deliberate: the locked design is brand-only, and PLA/PETG are format
  names rather than vendor styling.
- **Sticky lowercase typo**: typing a lowercase variant of a preset under "Other"
  creates a real vendor row that then suppresses that preset in the dropdown
  permanently. Accepted consequence of "typed input wins"; recoverable by renaming
  the vendor in Spoolman. A confirm-on-near-miss prompt was offered and not taken.
- **2.4.1 shows no "What's new" sheet** — the highlight copy did not change, and
  after U25 the sheet only opens when it has something new to say.

## SHIPPED 2026-08-28

- **Play accepted versionCode 116** and the upload is done (maintainer confirmed).
  So 116 is now a consumed code; the next release must use 117 or higher.
- **GitHub Release published**: https://github.com/ni4223/SpoolPainter/releases/tag/v2.4.1
  Live, not a draft, with `app-release.apk` (8,082,261 bytes) and `app-release.aab`
  (8,900,490 bytes) attached. Branch `v2` and tag `v2.4.1` pushed.
### Track baseline after this release — READ THIS BEFORE WRITING THE NEXT NOTE

**116 went to Open testing only** (maintainer, 2026-08-28). So:

| Track | Now on |
|---|---|
| Open testing | **2.4.1 (116)** |
| Production | **2.3.1** — unchanged |

**Production is now four versions behind**: 2.3.2, 2.4.0 and 2.4.1 have all shipped
to Open testing and none has been promoted. The **486-character production block in
this document is still unused and still correct** — it was written from the 2.3.1
baseline precisely for this promotion, so it is ready whenever the beta looks clean.

The next Open-testing note, by contrast, only has to cover what comes *after* 2.4.1.

> **Keep this table current.** The 2.2.1-vs-2.3.1 mix-up earlier in this document
> happened because a track baseline went stale and nobody wrote down what shipped
> where. It cost a rewrite of both blocks and nearly shipped a note advertising
> features stable users already had.

## Upload steps

1. `./gradlew :app:bundleRelease` → `app/build/outputs/bundle/release/app-release.aab`
   (no version bump needed — 116 was never uploaded)
2. Confirm Play accepts **116** (if it objects, bump and rebuild).
3. Paste the release-notes block above.
4. Roll out, then tag `v2.4.1` and publish the GitHub Release with the APK + AAB.
